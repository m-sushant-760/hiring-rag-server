# Web framework
fastapi==0.115.6
uvicorn[standard]==0.32.1

# File uploads
python-multipart==0.0.20

# PDF parsing
pymupdf==1.25.3          # PyMuPDF — best-in-class resume text extraction
python-docx==1.1.2       # DOCX support

# Vector database (Pinecone Integrated Inference — no separate embedding model needed)
pinecone==5.4.2

# LangChain (chunking utilities)
langchain>=0.3,<1.0

# Google Gemini — generation model (free tier via AI Studio)
google-genai>=2.0,<3.0

# Environment management
python-dotenv==1.0.1

# Testing
pytest==8.3.4
httpx==0.27.2            # FastAPI async test client
pytest-asyncio==0.24.0
